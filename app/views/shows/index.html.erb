<%# <ul>
    <li><%= link_to "Add a New Show", new_user_show_path(@user) %>
    <%# <li><%= link_to "Your Reviews", reviews_path %>
<%# </ul> %>

<h1>Shows Index</h1>

<ol>
    <% @shows.each do |show| %>
    
        <li>
            <b>Title:</b> <%= show.title %> 
            <b>- Genre:</b> <%= show.genre %> 
            <b>- Rating:</b> <%= show.reviews.first.rating %>
            <b>- Comment:</b> <%= show.reviews.first.comment %>
            <%# <%= link_to "Edit This Show", edit_show_path(show) %> %>
            <%# <%= link_to 'Delete', show_path(show), 
            method: :delete, data: { confirm: 'Are you sure?' } %> %>
        </li>
        </br>
    <% end %>
</ol>

